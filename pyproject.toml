[project]
name = "uv-template"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["./src/"]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "ruff>=0.15.20",
]
